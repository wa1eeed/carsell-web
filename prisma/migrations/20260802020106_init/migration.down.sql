-- نقض الترحيل الأول.
--
-- **هذا يمحو المخطط كلّه.** لا يُنفَّذ على قاعدة فيها بيانات إلا بعد نسخة
-- محقَّقة بـrestore-drill — انظر docs/RUNBOOK.md.
--
-- والنقض هنا حالة نظرية: نقض الترحيل الأول يعني العودة إلى قاعدة فارغة،
-- وهو ما تفعله إعادة البناء من الصفر بأمان أكبر. مكتوب للاكتمال لا للاستعمال.

DROP SCHEMA public CASCADE;
CREATE SCHEMA public;
