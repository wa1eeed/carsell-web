-- نقض: `Order.createdAt`.
--
-- **يمسّ المال**: العمود على جدول الطلبات.
--
-- النقض آمن هنا وهذا نادر: `createdAt` أُضيف ولم يُعبَّأ من مصدر خارجي، بل
-- من `stageEnteredAt` الباقي. فحذفه لا يفقد معلومة لا توجد في مكان آخر —
-- يفقد **دقّة**: `stageEnteredAt` يتصفّر عند كل انتقال مرحلة، فبعد النقض
-- لا يبقى ما يقول متى أُنشئ الطلب.
--
-- وأي تقرير زمني بُني على `createdAt` سيقرأ `stageEnteredAt` بعده ويعطي
-- أرقامًا مختلفة صامتة. راجع docs/api/account.md قبل التنفيذ.

ALTER TABLE "Order" DROP COLUMN IF EXISTS "createdAt";
