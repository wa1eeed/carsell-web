/**
 * ═══ قواعد الإعلان — ثوابتٌ في ملفٍ بلا `db` ═══
 *
 * تقرؤها شاشة الأدمن (A30) **وسيقرؤها مسار العرض حين يُبنى**. وكتابتها
 * في الشاشة تجعل الشاشة تقول «٤» ويُفرض الخادمُ غيرها — وهو بالضبط
 * الوعد المُخلَف الذي تحذّر منه القاعدة.
 *
 * ═══ ولا يُعرض إعلانٌ اليوم ═══
 *
 * لا مسار يقرأ `AdSlot` ولا `AdCampaign` خارج لوحة الأدمن: المساحات
 * مزروعة والأسعار مضبوطة **ولا إعلان يصل مستخدمًا**. والشاشة تقول ذلك
 * صراحةً بدل أن تعرض «إشغالًا» و«إيرادًا» لشيءٍ لم يُعرض قطّ.
 */

/** ٤ إعلانات كحدّ أقصى في جلسة الرئيسية — والسقف يُفرَض في الخادم لا في التطبيق. */
export const HOME_SESSION_AD_CAP = 4;

/** بنر واحد لكل شاشة كحدّ أقصى. */
export const MAX_BANNERS_PER_SCREEN = 1;

/** مسارات لا يدخلها إعلان — والدفع والمزايدة والنزاع لحظاتُ قرارٍ لا سوق. */
export const AD_FREE_PATHS = ['payment', 'bidding', 'dispute'] as const;

/** أحداث الظهور تُجمَّع وتُرسل كل ٣٠ ثانية — بطاريةً وشبكةً. */
export const IMPRESSION_FLUSH_SECONDS = 30;

/** تخزين استجابة المساحات ١٥ دقيقة. */
export const AD_CACHE_MINUTES = 15;

/**
 * **هل يعرض المنتج إعلانًا؟**
 *
 * ثابتٌ صريح لا استنتاج: حين يُبنى مسار العرض تُقلب هذه إلى `true`
 * في الالتزام نفسه — فلا تبقى الشاشة تقول «لا يُعرض» بعد أن صار يُعرض.
 */
export const ADS_SERVED = false;
