/**
 * قواعد إسناد الطلبات — **ثوابتٌ في ملفٍ بلا `db`**.
 *
 * تقرؤها الشاشة (`'use client'`) والنطاق معًا، فوضعُها في وحدةٍ تستورد
 * `db` يجرّ Prisma إلى حزمة المتصفّح ويُسقط البناء. (البوابة
 * `checkClientDbImports`.)
 */

/**
 * تجاوز الالتزام مرّتين في شهر يُسقط المزوّد من الإسناد الآليّ حتى
 * مراجعته — **والعميل لا يتحمّل تأخير مزوّد**.
 */
export const SLA_BREACHES_BEFORE_SUSPENSION = 2;
