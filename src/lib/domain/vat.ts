/**
 * **دُمج في `tax.ts`** (المهمة ٣٥) — والنسبة تُقرأ من `TaxRule` لا من
 * ثابت هنا. وهذا الملف يبقى غلافًا لحظةً حتى يتحوّل مستدعوه، ثم يُحذف.
 *
 * وبقاؤه ملفًّا حقيقيًّا كان سيجعل للنسبة مصدرين — وهو ما بُنيت
 * البوابة ١٦ لمنعه.
 */
export { DEFAULT_VAT_PCT, netOfVat, vatIncluded } from './tax';
